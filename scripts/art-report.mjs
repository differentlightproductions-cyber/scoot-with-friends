import {readFileSync,writeFileSync,mkdirSync,cpSync} from 'node:fs';
import path from 'node:path';
const target=path.resolve('../../artifacts/art-direction');
mkdirSync(target+'/release',{recursive:true});
cpSync('artifacts/art-direction/after',target+'/release',{recursive:true});
cpSync('artifacts/art-direction/paired',target+'/paired',{recursive:true});
const metrics=JSON.parse(readFileSync('artifacts/art-direction/paired/metrics.json','utf8'));
const picture=(src,label)=>`<figure><img src="${src}" loading="lazy" alt="${label}"><figcaption>${label}</figcaption></figure>`;
const rows=metrics.results.map(r=>`<tr><td>${r.label}</td><td>${r.frameMedianMs.toFixed(1)} ms</td><td>${r.frame95Ms.toFixed(1)} ms</td><td>${r.drawCalls}</td><td>${r.triangles.toLocaleString()}</td><td>${r.textures}</td><td>${r.loadMs} ms</td><td>${r.animationMedianMs.toFixed(2)} ms</td></tr>`).join('');
const poses=[['02-front','Front'],['03-side','Side'],['04-hardware','Deck, neck and fork'],['05-shoes-hands','Shoes and hands'],['06-standing','Standing'],['07-crouch','Crouch'],['08-grab','Deck grab'],['09-underside','Underside']];
writeFileSync(target+'/index.html',`<!doctype html><html lang="en"><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Scoot with Friends — visual update</title><style>body{margin:0;background:#132a2d;color:#e8eee7;font:16px/1.6 system-ui}main{max-width:1250px;margin:auto;padding:32px}h1{font-size:42px;line-height:1.1}h2{margin-top:42px}a{color:#a6efd7}.pair{display:grid;grid-template-columns:1fr 1fr;gap:12px}figure{margin:0 0 18px;background:#213b3e;border-radius:8px;overflow:hidden}img{display:block;width:100%}figcaption{padding:10px 16px}table{border-collapse:collapse;width:100%;font-size:14px}td,th{padding:10px;border-bottom:1px solid #4b6669;text-align:left}.scroll{overflow:auto}p{max-width:960px}@media(max-width:700px){.pair{grid-template-columns:1fr}main{padding:18px}h1{font-size:32px}}</style><main><p>SCOOT WITH FRIENDS · VISUAL UPDATE · 14 SEPTEMBER 2026</p><h1>Lazer, built from real scooter references</h1><p>The same scooter assembly appears while riding and in customization: a beveled deck, continuous shaped neck, paired fork blades, open wheel cores, ribbed grips, clamp hardware and a curved brake. Your supplied photos informed construction and proportions. No third-party names or logos are used. Lazer remains the initial brand.</p><p><a href="https://scoot-with-friends.nicsoundcloud22.chatgpt.site/?map=outdoor">Play the public game</a></p><h2>Same riding view, before and after</h2><div class="pair">${picture('paired/before.png','Previous published art')}${picture('paired/high.png','Updated art · High')}</div><h2>The actual gameplay scooter</h2><div class="pair">${picture('release/scooter-side.png','Lazer assembly · side')}${picture('release/scooter-front.png','Lazer assembly · front')}${picture('release/scooter-rear.png','Rear wheel, dropout and brake')}${picture('release/scooter-under.png','Underside and construction')}</div><h2>Rider and scooter comparisons</h2><p>These are captured from the running game. Each row uses matching inspection camera positions; the earlier images show the previous art in the working development build. The matched riding benchmark above compares the exact previously published build.</p>${poses.map(([file,label])=>`<h3>${label}</h3><div class="pair">${picture('before/'+file+'.png','Before')}${picture('release/'+file+'.png','After')}</div>`).join('')}<h2>Low / Medium / High</h2><p>Player geometry retains the same silhouette. Low reduces resolution, material detail and vegetation distance and disables shadows. Medium restores detailed materials and shadows. High increases shadow resolution, filtering and nearby foliage detail. These choices leave simulation and collision untouched.</p><div class="pair">${picture('paired/low.png','Low')}${picture('paired/medium.png','Medium')}${picture('paired/high.png','High')}${picture('release/bench-wood-rail.png','Bench planks, fasteners and coping')}</div><h2>Measured desktop performance</h2><p>Intel Core i5-13500, NVIDIA RTX 4060 Ti, Windows Chrome 153 headless, Direct3D11, 1440 × 900 viewport. Low renders at 0.85 pixel ratio; Medium and High at 1 on this display. Identical mapped push sequence and camera, 10 warm-up steps, 80 measured steps with a blocking GPU pixel read. These short local measurements are not sustained frame-rate or phone benchmarks. Load time includes local development page startup and varies with caching.</p><div class="scroll"><table><thead><tr><th>Build</th><th>Median frame</th><th>95th frame</th><th>Draw calls</th><th>Triangles</th><th>Textures</th><th>Local load</th><th>Median animation</th></tr></thead><tbody>${rows}</tbody></table></div><p>Software-only rendering was much slower and is not a practical substitute for an accelerated GPU. Physical Android/iPhone and Bluetooth-controller tests were not available in this pass.</p><h2>Verification and scope</h2><p>Build passed; 32 unit tests and 54 browser art checks passed. Checks cover preserved collider identities/transforms and timestep, loadout colorways, shared assembly, deformed clothing, grip alignment, and sampled completed hop input across both control profiles and stances. The published physics, input, scoring, grind and camera source is unchanged in this visual release. Existing gameplay issues and the unfinished shop/mobile feature work remain separate; this report does not claim that earlier backlog is complete.</p><p><a href="paired/metrics.json">Raw desktop measurements</a></p></main></html>`);
console.log(target+'/index.html');
